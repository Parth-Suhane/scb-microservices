import React from 'react';
import RegistrationForm from './RegistrationForm';

const Form = () => {
    return (
        <div>

        <RegistrationForm/>
            
        </div>
    );
};

export default Form;